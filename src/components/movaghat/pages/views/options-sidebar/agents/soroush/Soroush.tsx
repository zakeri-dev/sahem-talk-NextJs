import React from 'react'
import ReactMarkdown from 'react-markdown'

const SoroushMarkdown = () => {
  const markdownContent = `
خلاصه
  `

  return <ReactMarkdown>{markdownContent}</ReactMarkdown>
}

export default SoroushMarkdown
